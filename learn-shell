ls -lart
uname -a
pwd
ls -lart
##sample code edit to test
ps -ef|grep monogdb
##this is new change ill be setting again once more

#!/bin/bash
#!/usr/bin/env bash

# List files
ls

# cat user file
cat /etc/passwd | head -2

# grep root user from user file
grep root /etc/passwd

echo lets start fresh second attempt